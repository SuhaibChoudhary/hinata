<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome Settings</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1>Welcome Settings</h1>

        <form action="/dashboard/<%= guild.id %>/welcome" method="POST">
            <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" id="isEnabled" name="isEnabled" <%= isEnabled ? 'checked' : '' %>>
                <label class="form-check-label" for="isEnabled">Enable Welcome Feature</label>
            </div>

            <div class="mb-3">
                <label for="welcomeMessage" class="form-label">Welcome Message:</label>
                <textarea class="form-control" id="welcomeMessage" name="welcomeMessage"><%= welcomeMessage %></textarea>
            </div>

            <div class="mb-3">
                <label for="welcomeChannel" class="form-label">Welcome Channel:</label>
                <select value="<%= channel%>" name="welcomeChannel" class="form-select form-control">
                    <option value=""><%= channel %></option>
                    <% guild.channels.cache.filter(channel => channel.type === 0).forEach(channel => { %>
                    <option value="<%= channel.id %>">
                        <%= channel.name %>
                    </option>
                    <% }) %>
                </select>
            </div>

            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-P4ssLkEsDL/2UHFXyXtrpByBLiXbsSQc4hDlMDlU7BRuSZosL9cQxqERBQmPj9pf" crossorigin="anonymous"></script>
</body>
</html>
